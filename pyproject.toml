# Minimal pyproject.toml (adjust as needed)

[project]
name = "Glosc-Python"
version = "1.3.1"
description = ""
authors = [{ name = "xiaom" }]
requires-python = ">=3.10,<3.14"
dependencies = [
  "mcp",
  "markitdown[all]",
  "httpx"
]
